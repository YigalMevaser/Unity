apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-mongodb.fullname" . }}
  labels:
    {{- include "my-mongodb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-mongodb.selectorLabels" . | nindent 4 }}
